import 'package:flutter/material.dart';

class HomeParkingMan extends StatelessWidget {
  const HomeParkingMan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
